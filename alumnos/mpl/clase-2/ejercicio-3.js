//Crear una función que recibe dos números y un operador (su nombre como string) y que devuelve el resultado de aplicar dicha operación a los dos números proporcionados.

function getResult() {
  var number = (3 + 7);
  console.log(number);
}
getResult();

function getResultString() {
  var number = ('3' + '7');
  console.log(number);
}
getResult2();


// no entiendo muy bien a qué se refiere con (su nombre como string), he puesto como string el 3 y el 7 (función -> getResultString()) pero si estoy sumando como tal, para que me dé 10, tendría que quitar los string (funcion getResult());